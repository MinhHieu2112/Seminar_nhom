FiveS Prompt:

1) Situation:
Bạn là senior full-stack engineer. Hãy xây dựng `scheduler-service` theo kiến trúc Microservices cho hệ thống quản lý lịch học và công việc.

2) Scope:
Backend dùng Node.js + TypeScript + NestJS + Prisma + PostgreSQL. Scheduler-service phải có database riêng tên `scheduler_db` nhưng dùng chung container PostgreSQL hiện tại để tiết kiệm tài nguyên Docker và giảm dung lượng hệ thống.

3) Structure:
Thiết kế schema gồm:
- Categories
- Subjects
- Schedules
- Tasks
- TaskAllocations
- UserPreferences

Yêu cầu:
- scheduler-service chỉ lưu `userId`, không join trực tiếp với user-service
- giao tiếp user-service qua internal HTTP API
- dùng environment:
  USER_SERVICE_URL=http://user-service-app:8001
- cập nhật docker-compose để các service giao tiếp bằng service name
- tạo migration, DTO, validation, repository, service, controller, API CRUD đầy đủ
- tối ưu cho time-blocking, recurring schedule và drag-drop task allocation

4) Style:
Frontend thiết kế theo ảnh mẫu: sidebar trái, weekly calendar grid ở giữa, task panel bên phải, pastel minimal UI, modern productivity app style, hỗ trợ kéo-thả task vào lịch.

5) Success:
Output đầy đủ:
- architecture
- Prisma schema
- docker-compose
- API endpoints
- code mẫu runnable
- giải thích rõ logical separation trong cùng PostgreSQL container để đúng chuẩn microservices nhưng tiết kiệm RAM/disk.